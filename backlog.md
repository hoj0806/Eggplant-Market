# 남은 일 — 미구현 · 범위 밖 모음

기준: `03f9ab9`(develop, 0035까지 머지) · 갱신 2026-08-07 (비밀번호 재설정까지 반영)

> 갱신 2026-08-05 — 처음 쓸 때 로컬 `develop`이 2커밋 뒤처져 있어 **8-1(비밀번호 변경·회원탈퇴)을
> 남은 일로 적었으나 이미 PR #18로 머지된 상태였다.** 아래는 바로잡은 내용이다.
> 이후 PR #20(댓글 1단)·#22(댓글·찜 알림)·대댓글의 결과를 반영했다. §2-1과 §3-2에는
> **처음에 잘못 적은 예상**("마이그레이션 없이 시작 가능", "트리거만 더하면 된다")과
> 실제로 무엇이 더 필요했는지를 함께 남겼다.

`todo.md`에 남은 단계, `feature.md` 명세 중 안 지킨 것, `note.md` 12개 절의 "이번 범위 밖"을
한자리에 모았다. 각 단계를 끝낼 때마다 뒤로 미룬 것들이 열두 군데에 흩어져 있어서,
지금 무엇이 남았는지 한 번에 볼 수 없었다.

**옛 "범위 밖" 중 이미 해소된 것은 뺐다.** 2~7단계가 지나가며 대부분 메워졌다(§6).
남은 것만 아래 다섯 절에 있다.

읽는 법 — §1·§2는 **하기로 되어 있는데 안 한 것**, §3은 **깔아만 두고 안 쓰는 것**,
§4는 **판단이 끝나 손만 대면 되는 것**, §5는 **사람이 기준을 정해야 시작되는 것**이다.
§5를 §4처럼 착수하면 중간에 멈춘다.

---

## 1. 남은 단계 — `todo.md` 8단계

8-1은 끝났다. 남은 것은 카카오 로그인 하나다.

### 1-1. 비밀번호 변경 · 회원탈퇴 — ✅ 완료 (PR #18, 2026-08-05)

`/settings/account` 신설, 첫 Edge Function `delete-account` 배포까지 끝났다.
구현 노트는 `note.md`의 "계정 — 비밀번호 변경 · 회원탈퇴".

**남은 검증 하나**: 실제 계정 삭제를 앱에서 한 번도 밟아 보지 못했다(진짜로 지워도 되는 계정이
필요하다). 탈퇴 후 로그인 화면으로 밀려나는지, 그 이메일로 다시 가입되는지, 버킷에 파일이
남지 않았는지가 남은 확인이다.

**딸려 나온 수정** — ✅ 완료 (`0016_review_delete.sql`): 탈퇴가 cascade로 후기를 지우는데
`recalc_manner_temp`가 insert 전용이라 상대 매너온도가 어긋난 채 남았다. 파 보니 같은 구멍을
**게시물 삭제(2단계)가 하루 먼저 열어 두고 있었다.** 후기 삭제 트리거를 붙이고 백필했다.

### 1-2. 카카오 로그인

- **현황**: 명세는 카카오·구글·이메일인데 실제는 구글·이메일뿐이다.
- **설계**: 코드는 `googleSignInButton.tsx` 복제 수준으로 적다.
- **마이그레이션**: 없음
- **걸림돌 — 사람이 먼저 해야 한다**: 카카오 개발자 콘솔 앱 등록, REST API 키 발급,
  Supabase Auth 프로바이더 설정, 리다이렉트 URI 등록. **이 준비 없이는 코드를 짜도 확인이 안 된다.**
  착수 전에 준비 여부부터 확인한다.
  (`authErrorMessage.ts`가 이미 "해당 소셜 로그인이 아직 활성화되지 않았습니다"를 갖고 있다.)

---

## 2. 명세에 있는데 없는 것 — `feature.md`

**명세 대비 구멍은 §1의 카카오 로그인 하나만 남았다.** 댓글도 지도도 닫혔다.

### 2-1. 게시물 댓글 — ✅ 완료 (1단 PR #20 `0017_comment.sql` · 대댓글 2026-08-05)

- **1단 댓글**: `src/features/comment/`가 `.gitkeep` 하나뿐이었으나 api/hooks/components·정책까지
  붙었다. 목록·작성·삭제(작성자 + 게시물 판매자)·차단 숨김.
- **여기 적었던 "마이그레이션 없이 시작 가능"은 틀렸다.** 0001의 `comments_select`가
  `using (true)`라 **6단계 차단이 댓글만 비껴가고 있었고**, `content`도 자유 text였다.
  0013·0014에서 되풀이된 자리다 — 0001의 정책은 그 칸을 쓰는 화면이 생길 때 다시 읽어야 한다.
- **대댓글**: 붙였다. **마이그레이션이 없다** — 여기 적어 둔 예상대로 0017의 정책도 0018의
  알림도 그대로 받았다. 화면에서 트리를 접고(`buildCommentTree`), 답글 버튼을 1단에만 두고
  (**2단 고정** — 깊이를 열면 0018이 알리는 사람과 화면이 어긋난다), 부모가 지워질 때는
  cascade를 유지하되 몇 개가 함께 사라지는지 미리 알린다.
  - **딸려 나온 수정**: 0017의 삭제 캐시가 지운 id 하나만 걷어내고 있었다. 1단만 있을 때는
    정확했지만 답글이 생기는 순간 **틀린 코드가 됐다** — 서버는 cascade로 답글까지 지우는데
    캐시에는 남아, 부모를 잃은 답글이 1단으로 떠오른다. 한 줄 더 봐서 닫았다.

### 2-2. 지도에서 주변 물품 보기 — ✅ 완료 (`0025_region_counts.sql`, 2026-08-06)

`/search/map`에 붙었다. **마커 하나가 동네 하나**다(자세한 것은 §5-1).
구현 노트는 `note.md`의 "지도 — 동네 단위 마커".

**브라우저에서 동작 확인함**(2026-08-06). 타일·마커·반경 원까지 실제로 그려진다.

---

## 3. 깔아만 두고 안 쓰는 것

동작하는 코드/스키마가 있는데 부르는 데가 없다. 만드는 비용이 아니라 **잇는 비용**만 남은 것들이다.

### 3-1. `nearby_posts` RPC · `profiles.search_radius_m` · 지도 렌더링 — ✅ 완료

`0024_radius_search.sql`(반경) · `0025_region_counts.sql`(지도), 2026-08-06. **이 절은 닫혔다.**

| 조각 | 상태 |
|---|---|
| `nearby_posts(...)` RPC | ✅ **지웠다**(0024). 살리지 않은 이유는 아래 |
| `profiles.search_radius_m` (기본 2000) | ✅ 읽고 쓴다. 설정 UI는 `/settings/region` |
| `posts.location` (geography) | ✅ 반경 검색·지도 집계의 기준으로 쓰인다 |
| Kakao SDK 로더 | ✅ **손대지 않았다** — 아래 참고 |
| 지도 그리기 | ✅ `/search/map`. `shared/types/kakaoMaps.ts`에 `Map`·`Circle`·`CustomOverlay`를 더했다 |

**여기 적었던 예상 둘이 다 틀렸다.**

1. **"호출 0회니까 잇기만 하면 된다"** — `nearby_posts`는 0001이 아무 화면도 없을 때 적어 둔
   함수라 그동안 목록에 쌓인 규칙을 하나도 모른다(차단 제외 0014 · 검색어·가격 필터 0007 ·
   `(정렬값, id)` keyset 0011 · 목록 카드의 반환 모양). 채워 넣으면 `search_posts`와 같아지고,
   같아진 둘을 나란히 두면 0014가 실제로 치른 값(규칙 하나에 네 곳 수정)을 되풀이한다.
   그래서 **기준을 함수가 아니라 인자로 갈랐다.**
2. **"로더 옵션(`libraries`)부터 넓혀야 한다"** — 넓힐 것이 없었다. SDK 부트스트랩이
   `o = ["v3"]`로 시작해 **지도 API를 언제나 싣고**, `libraries`는 거기에 **더** 얹는 옵션이다
   (소스에서 확인했다). 넓힐 것은 타입 선언뿐이었다.

**0025가 한 일 하나 더**: 0024가 `search_posts` 안에 두었던 WHERE를 `private.posts_in_scope`로
빼서 지도 집계와 공유한다. 조건이 갈리면 마커의 숫자와 눌러 들어간 목록이 어긋나기 때문이다 —
특히 차단이 그렇다. 순수 SQL로 적어 계획기가 인라인하므로 `st_dwithin`은 여전히 GiST를 탄다.

### 3-2. `notification_type`의 `comment` · `like` — ✅ 완료 (PR #22, `0018`)

- 트리거 둘(`notify_post_commented`·`notify_post_liked`)이 붙어 enum 다섯 값이 전부 쓰인다.
- **"트리거만 더하면 된다"도 절반만 맞았다.** 0015가 풀어 둔 것은 게시물·미리보기 **자리**까지였고
  `actor`는 `coalesce(m.sender_id, rv.reviewer_id)`라 댓글·찜에서 비었다. 그대로 붙였으면
  전부 "알 수 없는 이웃님이"가 됐을 것이고, 폴백이 정상 동작인 척 덮어 버려 **버그로 보이지도 않는다.**
- payload에 `actor_id`를 넣어 해결했고, 덤으로 차단 청소가 타입을 묻지 않는 갈래 하나로 줄었다.
- 화면은 한 줄도 안 고쳤다 — 낡은 주석만 고쳤다.

### 3-3. 그 밖

- **`reviews.manner_tags`(`text[]`)** — 쌓기만 하고 세지 않는다. 당근처럼 "시간 약속을 잘 지켜요 12"를
  보이려면 `unnest` + `group by`면 되지만, 후기가 쌓이기 전에는 볼 것이 없다.
- ~~**`posts.updated_at`**~~ — ✅ 붙였다(`0020`). 붙이는 김에 **그 값이 상태 변경에도 오르고 있다는 것**을
  발견해 뜻부터 좁혔다. §4의 "수정됨 표시" 항목을 보라.

---

## 4. 일부러 뺀 것 — 판단은 끝났다

각 단계에서 "이번 범위 밖"으로 넘긴 것 중, **무엇을 할지는 이미 정해졌고 손만 대면 되는** 것들이다.
괄호 안은 미룬 단계.

### 게시물

- **"수정됨" 표시** (2단계) — ✅ 완료 (`0020_edited_mark.sql`, 2026-08-05).
  **"한 줄 붙이면 된다"는 틀렸다.** `updated_at`이 쌓이고는 있었지만 **상태 변경에도 올랐다**
  (0008이 그렇게 정했고, 읽는 화면이 없을 때는 맞는 판단이었다). 예약중으로 바꿨을 뿐인데
  "수정됨"이 뜨면 글이 바뀐 줄 안다. 0005·0010이 이어 온 **제외 목록을 포함 목록으로
  뒤집어** 뜻을 좁혔다 — 앞으로 칸이 늘어도 기본값이 "수정 아님"이다.
  이미 쌓인 세 건은 되돌리지 않았다(내용 수정이었는지 되짚을 방법이 없다).
- **끌올 남은 시간 실시간 갱신** (2단계) — ✅ 완료 (2026-08-06, 마이그레이션 없음).
  `shared/hooks/useNow`가 1분마다 흐른다(문구가 분 단위까지만 적으므로 그보다 자주 흐를 이유가 없다).
  홈·검색 목록, 마이페이지 넷, 상세의 ⋯ 메뉴가 함께 살아났다 — "3분 전"도 이제 멈추지 않는다.
- **수정 시 동네 변경** (2단계) — 안 바꾸기로 했다. 올린 뒤 이사를 갔더라도 그 글이 그 동네
  글이었다는 사실은 그대로다. **재검토 대상 아님.**

### 탐색

- **홈의 정렬 선택** (3단계) — 홈은 최신순이 전제다. 순서를 고르고 싶은 순간에는 이미 찾는 것이
  있으므로 검색이 맡는다. **재검토 대상 아님.**

### 채팅 · 가격 제안

- **제안 수락 → 예약 자동 전환** (4단계) — 수락이 곧 거래 약속은 아니다. 상태는 판매자가
  `postStatusControl`에서 직접 바꾼다. **재검토 대상 아님.**
- **여러 제안 히스토리 화면** (4단계) — 대화 흐름 안의 말풍선으로 충분하다고 봤다.
  취소(0027)가 생기면서 "몇 번 부르고 몇 번 물렸나"가 오히려 대화에 그대로 남는다.
- **메시지 삭제 · 채팅방 나가기/숨기기** (3단계에서 미룸) — ✅ 완료 (`0029` · `0030`, 2026-08-06).
  **여기 적어 둔 "칸이 필요할 수 있다"는 맞았다** — `buyer_left_at`·`seller_left_at` 둘이
  붙었다. 다만 §5로 넘어가지는 않았다. 정할 것이 하나씩뿐이었다.
  - **삭제는 소프트 삭제**다. 행은 남고 `content`만 사라진다 — 0008이 막은 것은 **상대의
    말을 고치는 일**이었고 여는 것은 **내 말을 무르는 일**이라 그 선을 넘지 않는다.
    덤으로 상대 화면에 **UPDATE로 도착**해 `withUpdatedMessage`가 그대로 받는다(캐시 경로 0개 추가).
    **가격 제안은 못 지운다** — 그 자리는 0027의 취소가 맡고 있고, 열면 수락된 제안을 지워
    합의를 없앨 수 있다.
  - **나가기는 목록에서만 감춘다.** 상대가 다시 말을 걸면 돌아온다(where 조건 한 줄, 트리거 없음).
    안 돌아오게 하는 것은 나가기가 아니라 차단(0014)이 할 일이다. 나간 방도 **주소로는 열린다** —
    안 열면 게시물 상세의 "채팅하기"가 막다른 길이 된다. 기준은 `fetch_chat_rooms`의 인자로 갈랐다(0024와 같다).
  - **가장 값이 나간 발견**: update 정책에 `with check`가 없으면 Postgres가 `using` 식을
    **새 행에도 그대로 쓴다.** 0008·0027이 `using`만으로 멀쩡했던 것은 그 식이 보는 칸을
    아무도 안 바꿨기 때문이고, `deleted_at`을 조건에 넣는 순간 **삭제가 자기 정책에 걸렸다.**
    자세한 것은 `troble.md`.
  - **딸려 나온 것**: `SafetyMenu`에 `extraItems` 슬롯(헤더 한 줄에 ⋯를 둘 놓을 수 없다) ·
    `chatErrorMessage` 패턴을 0027이 겪은 자리에서 또 고침(문장 끝 대신 앞부분을 잡게 바꿨다) ·
    방 요약(`last_message`)과 안 읽은 수에서 지운 줄을 빼는 일.
  - **화면은 아직 눈으로 못 봤다.**
  - **새로 미룬 것**: 채팅 목록에서 바로 나가기 · "대화 내용 모두 지우기".

- **채팅방 완전 삭제** — ✅ 완료 (`0031_chat_room_purge.sql`, 2026-08-06).
  **사용자가 누르는 "삭제 버튼"은 안 만들었다** — 한쪽이 누르면 상대의 기록까지 사라져
  0008의 선을 넘고, 방금 만든 소프트 삭제·나가기가 그 선을 지키려고 고른 모양과 어긋난다.
  대신 **양쪽이 다 나간 방을 조용히 지운다.** 아무도 볼 수 없는 방이라 잃는 사람이 없고
  사용자가 결정할 것도 없다.
  - **"둘 다 나갔다"는 시각 둘이 찍혔다는 뜻이 아니다** — 0030에서 방은 나간 뒤에 말이 오면
    돌아온다. 조건을 `private.is_chat_room_purgeable` 한 군데에 적고 셋이 나눠 쓴다
    (나가기 · 지우기 · 사진 정리).
  - **밟아 보고 안 것**: 방을 지우면 **`posts_update`가 잠긴다.** 0008이 `with check`로
    "거래 상대는 채팅한 사람 중에서만"을 지키는데 그 근거가 `chat_rooms` 행 하나라,
    예약중인 글의 판매자가 제목조차 못 고치게 된다. **거래 상대로 걸린 방은 안 지우는 쪽**으로
    좁혔다(정책을 트리거로 옮기는 길은 아래 새 항목).
  - **알림에는 cascade가 없다**(payload가 jsonb). 방이 사라지면 갈 곳 없는 알림이 남아
    `purge_chat_room`이 함께 지운다. 0015가 차단에서 한 것과 같은 자리다.
  - **사진은 DB가 못 지운다** → 순서가 규칙이 됐다: **파일 먼저, 행 나중.** 반대로 하면
    방이 사라진 뒤라 목록조차 못 읽는다. `chat_images_delete`를 지울 수 있는 방에 한해 한 겹 열었다.
  - **딸려 나온 것**: 메뉴 항목 클래스가 `shared/ui/menuItem`으로 올라갔다 — 문자열 하나
    때문에 chat이 block 기능 전체와 supabase를 끌고 오던 것을 테스트가 잡아냈다(`troble.md`).
  - **새로 미룬 것**: ~~`posts_update` 판정을 트리거로 옮기기~~ (✅ `0035`) ·
    ~~게시물 삭제 때 알림·사진 정리~~ (✅ `0032`) · 끊긴 뒷정리 다시 시도하기.

- **거래 상대 판정을 정책에서 트리거로** — ✅ 완료 (`0035_trade_partner_guard.sql`, 2026-08-07).
  0031이 길까지 적어 둔 부채인데, **옮기러 갔다가 구멍을 찾았다. 옮기는 일보다 그쪽이 크다.**
  - **정책이 update에만 걸려 있었다.** 0008은 "거래 상대는 채팅한 사람 중에서만"을
    `posts_update`에 두었는데 `posts_insert`는 0001 그대로였다. 그래서 **채팅한 적 없는
    이웃을 구매자로 박은 `sold` 글을 새로 만들고**, 그걸 근거로 −0.5 후기를 꽂아
    **남의 매너온도를 깎을 수 있었다**(평범한 계정으로 밟아 확인, 36.5 → 36.0).
    글은 얼마든지 만들 수 있어 되풀이된다.
  - **원인은 도구 선택이다.** 정책은 명령마다 따로 적으므로 **한 규칙이 두 명령에 걸려야 하면
    정책은 잘못된 도구다.** `before insert or update` 트리거로 한 번에 걸었다.
  - **0031의 부채는 "안 바뀌면 안 묻는다" 두 줄이 갚았다.** 이제 근거가 된 방이 사라져도
    판매자가 제목을 고칠 수 있고, `is_chat_room_purgeable`에서 거래 상대 조건을 뺐다.
  - **딸려 나온 것**: 오류가 42501에서 23514 + 한국어 문구로 바뀌면서 `postStatusControl`이
    쓰던 `toPostErrorMessage`에 안 걸려 **"잠시 후 다시 시도해 주세요"로 뭉개질 뻔했다**
    (0027·0029에 이어 세 번째다). `toPostActionErrorMessage`로 바꾸고 서버 문구를 그대로 넣은
    테스트를 붙였다.
  - `buyer_id`가 찬 글이 0건이라 되돌릴 옛 데이터는 없었다. 다른 환경을 위해 찾는 질의만
    주석에 적어 두었다 — 무엇을 할지는 사람이 정할 일이다.

- **게시물 삭제의 뒷정리** — ✅ 완료 (`0032_post_delete_cleanup.sql`, 2026-08-06).
  0031이 남긴 부채. 글을 지우면 FK cascade 여섯(채팅방·댓글·찜·사진·최근 본 글·**거래후기**)은
  따라가는데, **FK를 걸 수 없는 둘이 남고 있었다.**
  - **알림** — payload가 jsonb라 FK 자리가 없다. `before delete` 트리거로 걷어낸다
    (after면 cascade가 방을 이미 가져간 뒤라 채팅 알림을 못 찾는다). 트리거인 이유는
    **회원탈퇴도 그 사람의 글을 전부 지우기** 때문이다 — RPC에 적으면 그 길이 비껴간다.
    **0016은 그냥 뒀는데 이번엔 지우는 이유**: 그때는 게시물이 살아 있어 제목과 갈 곳이
    남았지만, 글이 사라지면 이름·제목·미리보기·링크가 **전부** 빈 줄이 된다.
  - **채팅 사진** — 0031의 문 둘이 다 안 맞아(본인 것만 / 방이 있어야 판단) 세 번째를 냈다:
    **방이 더는 없는 폴더.** 이미 아무도 읽을 수 없는 파일이라 열어 주는 것이 치울 권한뿐이다.
    덤으로 0031이 끊겨 남은 폴더와 옛 고아 파일도 이제 정리된다.
    순서가 0031과 **반대**다 — 0031은 방이 살아 있어야 지울 수 있고(파일 먼저), 0032는
    방이 사라져야 지울 수 있다(행 먼저). 목록은 양쪽 다 방이 살아 있을 때만 읽힌다.
  - **삭제 확인 문구에 거래후기를 더했다.** 사라지는 것 중 유일하게 이 글 밖에 자국을
    남기는 것이다. **"매너온도가 내려갑니다"는 틀렸다** — 밟아 보니 나쁜 후기가 사라지면
    올라간다(36.0 → 36.5). 0016이 합계로 다시 계산하기 때문이라 "되돌아갑니다"로 적었다.
  - **실제 채팅 사진으로는 못 밟았다** — 버킷이 비어 있다. 정책 판정식만 값으로 확인했다.
  - **새로 미룬 것**: 회원탈퇴 때 상대가 올린 채팅 사진(Edge Function 배포가 따라온다) ·
    옛 고아 파일 일괄 청소.

### 신뢰 · 안전

- **후기 수정** (5단계) — **안 열기로 했다** (2026-08-06). 0016이 `sync_manner_temp`를 만들면서
  기술적 걸림돌은 사라졌지만, 열면 **받은 사람이 본 문구가 나중에 바뀐다.** 후기는 거래의
  기록이지 고쳐 쓰는 글이 아니다 — 0008이 대화 기록에, 0027이 제안에 그은 선과 같다.
  마음이 바뀌면 남길 수 있는 것은 다음 거래의 후기다. **재검토 대상 아님.**
  - 후기를 **없애는** 길은 있다: 그 **게시물을 지우면 후기도 함께 사라진다**(cascade).
    이때 상대의 매너온도도 되돌아간다(0016). 삭제 확인 문구가 그 사실을 적는다(0032).
  - (후기 **삭제** 쪽 매너온도 어긋남은 0016이 닫았다.)
- **매너온도 이력** (0016) — ✅ 완료 (`0034_manner_temp_history.sql`, 2026-08-07).
  `manner_temp_events` 한 표와 `/my/manner` 화면. 마이페이지 프로필 카드의 온도를 눌러 들어간다.
  - **"왜"를 손으로 안 적었다.** `reason` enum 대신 **그 순간의 후기 개수·합계**를 남긴다.
    이유는 앞뒤 줄을 견주면 나오고(개수가 늘었나 줄었나), 무엇보다 근거가 있어야
    `after_temp = clamp(36.5 + review_sum)` 등식으로 **어긋남 자체를 잡을 수 있다.**
    온도를 99로 밀어 넣어 그 줄만 등식이 깨지는 것까지 확인했다. 손으로 붙인 이름은
    0018의 "알 수 없는 이웃님이"처럼 **틀려도 정상으로 보인다.**
  - **적는 자리는 `profiles`의 트리거다** — `sync_manner_temp` 안이 아니다. 0016이 밟은 실패가
    "insert에만 붙어 나가는 길을 아무도 안 봤다"였으므로, 기록은 바꾸는 자리보다 아래에 둔다.
  - **본인만 읽는다.** 온도는 공개지만 이력은 거래 시각·**사라진 후기의 자국**까지 말한다.
  - **백필은 못 한다.** 사라진 후기를 되짚을 방법이 없다 — 그 구멍이 이 표를 만든 이유다.
    그래서 빈 화면 문구도 "기록이 없어요"가 아니라 "아직 움직인 적이 없어요"다.
  - **화면은 아직 눈으로 못 봤다.**
  - **새로 미룬 것**: 이력 페이징(keyset 받을 인덱스는 만들어 뒀다) · 어긋난 줄을 화면에서
    알리기(겁만 주고 할 수 있는 일이 없다) · 후기 하나하나로 이어지는 길(`review_id`는
    FK를 걸 수 없어 두지 않았다).
- **첫 거래 안내** (5단계) — ✅ 완료 (`0021_first_review.sql`, 2026-08-05).
  **축하 "화면"은 안 만들었다.** 모달은 "이미 봤는가"를 적을 칸이 먼저 필요한데,
  알림은 그 상태(`is_read`)를 이미 갖고 있고 첫 후기 알림은 **이미 가고 있었다**(0001) —
  다른 후기와 구별되지 않았을 뿐이다. 새 통로 대신 그 줄에 표를 붙였다.
  "처음 받아 본 적 있는가"가 아니라 **"지금 한 건인가"**로 판단한다(0016이 온도를 다루는 방식).
- **차단한 사람 프로필 가리기** (6단계) — **가리지 않기로 했다.** 왜 차단했는지 확인하고 해제를
  판단하는 자리라 막을 이유가 없다.
- **찜·최근 본 글에서 차단 상대 걸러내기** (6단계) — **걸러내지 않기로 했다.** 내가 남긴 흔적이라
  상대를 차단해도 그대로 둔다(당근도 같다).
- **차단 사유 남기기** (6단계) — `blocks`에 사유 칸이 없다. 차단은 신고와 달리 남에게 설명할 일이
  아니다. **재검토 대상 아님.**
- **차단 해제 시 알림 되살리기** (7단계) — 되살리지 않는다. 알림은 "그때 알려 주는 것"이라
  되돌릴 값이 아니다(방과 대화는 0014대로 그대로 돌아온다).

### 앱 껍데기

- **글쓰기 탭 FAB** (1단계) — ✅ 완료 (2026-08-06). 어느 탭인지를 배열 가운데로 **계산하지 않고**
  값(`isPrimary`)으로 적었다 — 계산하면 자리를 더하거나 순서를 바꿀 때 엉뚱한 탭이 떠오른다.
  딸려서 `AppLayout`의 아래 여백을 `pb-16` → `pb-24`로 넓혔다(원이 24px 올라와 마지막 줄을 덮었다).
  **생김새는 아직 눈으로 못 봤다.**
- **채팅방·상세 헤더 통일** (1단계) — ✅ 완료 (2026-08-06, 마이그레이션 없음).
  세어 보니 **열 곳**이었다. 구조가 두 갈래(세로형 6 · 가로형 2)였는데 **한 틀로 덮였다** —
  `PageHeader`가 "첫 줄(링크 + 선택적 action) + 아래(선택적 제목·설명)"를 그리면 양쪽이
  지금과 똑같이 나온다. **틀을 맞추려고 생김새를 바꾸지는 않았다.**
  채팅방만 뺐다(한 줄에 아바타·이름·메뉴가 함께 있어 두 칸으로 안 나뉜다) — 그래서
  컴포넌트를 `BackLink`와 `PageHeader` 둘로 나눴고, 채팅방은 앞의 것만 쓴다.
  **파 보니 나온 것**: 채팅방 뒤로가기가 `←` 한 글자뿐이라 **이름 없는 링크**였다
  (스크린리더에 "왼쪽 화살표 링크"). `isLabelHidden`으로 글자만 감추고 이름은 남긴다.
  **목적지는 통일하지 않았다** — 화면마다 돌아갈 곳이 실제로 다르다. 말하는 방식만 손봤다
  (`← 게시물로 돌아가기` → `← 게시물`, "돌아가기"는 화살표가 이미 하는 말이다).
- **테마를 계정에 저장** (1단계) — 기기별로 두는 편이 낫다고 판단. **재검토 대상 아님.**

### 알림

- **알림 개별 삭제** (7단계) — ✅ 완료 (`0019_notification_delete.sql`, 2026-08-05).
  정말로 한 줄이었다. update와 달리 컬럼을 잠그는 트리거가 필요 없었다 —
  0015가 그것을 둔 이유는 **남는 행이 거짓말을 하게 되는 것**을 막기 위해서였고,
  삭제에는 남는 행이 없다. 새로 미룬 것은 "모두 삭제"(확인 절차가 따라온다) 하나다.
- **찜 취소 시 알림 회수** (0018) — 하지 않기로 했다. 알림은 "그때 알려 주는 것"이라
  0015가 차단 해제에 대해 정한 것과 같은 결이다. **재검토 대상 아님.**

### 댓글 (0017 · 0018 · 대댓글)

- **삭제된 부모를 "삭제된 댓글입니다"로 남기기** — 지금은 답글이 살아 있어도 부모와 함께
  사라진다(cascade). 남기려면 `deleted_at` 같은 칸과 그것을 아는 정책이 필요해
  **마이그레이션이 붙는다.** 지금은 함께 지워진다는 사실을 미리 알리는 쪽으로 갈음했다.
- **답글 접기/펼치기** — 답글이 여럿이면 부모 밑이 길어진다. 몇 개부터 접을지는 실제로
  쌓여 봐야 정해진다(댓글 페이징을 미룬 것과 같은 이유).
- **3단 이상 답글** — **열지 않기로 했다.** 0018의 알림이 부모 댓글 작성자 한 사람에게
  가므로, 깊은 답글을 루트로 접어 붙이는 흔한 방식을 쓰면 화면과 알림이 다른 곳을 가리킨다.
  열려면 트리거부터 고쳐야 한다. **재검토 대상 아님.**
- **댓글 수정** — ✅ 완료 (`0020_edited_mark.sql`, 2026-08-05). 예상대로 "수정됨"과 한 묶음이었다.
  **0017이 "정책만 남아 있는 것은 위험하지 않다"고 한 것은 절반만 맞았다** — 작성자 본인만
  통과하는 것은 맞지만, 본인이 `post_id`(다른 글로 옮기기)·`parent_id`(아무 댓글 밑으로
  붙이기)·`created_at`(맨 위로 올리기)을 바꿀 수 있었다. `guard_comment_update`로 닫았다.
  고치기는 **작성자 본인뿐**이다 — 삭제와 달리 판매자에게 열지 않았다.
- **게시물 카드의 댓글 수** — ✅ 완료 (`0028_comment_count.sql`, 2026-08-06).
  **비용을 다시 잰 것이 맞았다** — 컬럼·트리거·백필은 0005를 베끼면 되는 쉬운 쪽이었고,
  진짜 크기는 **목록 RPC 여섯을 전부 drop + create**하는 반복이었다(칸 하나 늘리는 데 300줄).
  **대댓글도 함께 센다** — 카드의 "댓글 3"은 그 글에 달린 말의 개수다.
  **0020이 값을 했다**: `posts_set_updated_at`이 포함 목록이라 손댈 필요가 없었다 —
  제외 목록이었으면 댓글이 달릴 때마다 "수정됨"이 떴을 것이다.
  **딸려 나온 변경**: 카드의 숫자 줄이 0인 항목을 빼고 적는다(`toPostCountsText`).
  셋이 되면서 "찜 0 · 조회 3 · 댓글 0"처럼 없는 것이 더 길어졌다.
  **남긴 부채**: 여섯이 같은 요약 모양을 베껴 쓴다. 합치는 일은 여섯을 한꺼번에 다시 짜는
  일이라 이번과 섞지 않았다 — 이 모양을 또 건드릴 일이 생기면 그때가 신호다.
- **댓글 페이징** — 지금은 한 글의 댓글을 전부 읽는다. 인덱스는 `(post_id, created_at)`이라
  keyset을 얹을 준비가 되어 있다. 댓글이 수십 개 달리기 전에는 볼 것이 없다.
- **댓글 알림 묶기** (0018) — 한 글에 댓글 열 개면 알림도 열 줄이다. 당근은 묶는다.
  목록 RPC가 아니라 **알림 모델 자체**를 건드리는 일이라 §5로 넘길 수도 있다.

---

## 5. 애매한 것 — 기준을 정해야 시작된다

여기 있는 것들은 코드 문제가 아니라 **결정 문제**다. 정하지 않고 손대면 중간에 멈춘다.

### 5-1. 반경 검색 · 거리순 · 지도 — ✅ 완료 (`0024` · `0025`, 2026-08-06)

- **정한 것: 병행이다.** 법정동 기준을 그대로 두고 검색 화면에 기준 전환을 붙였다
  (`0024_radius_search.sql`, 2026-08-06). 거리순도 함께 열렸다 — 다만 **반경 기준에서만**이다.
  기준을 함수가 아니라 `search_posts`의 인자로 갈랐다(§3-1).
- **가장 큰 발견: 반경이 고르는 것은 물건이 아니라 동네다.** `posts.location`이 판매자 동네의
  대표 좌표라(0005) 같은 동 글은 좌표가 전부 같다. 실제 데이터 27건의 거리가 모두 같았다.
  그래서 정렬 이름을 **"가까운 동네순"**으로 적었다 — "가까운 순"은 물건마다 거리를 재 준다는
  뜻이 되어 거짓말이 된다. 이 사실이 지도의 마커 설계도 정했다(아래).
- **딸려 나온 것**: 거리순은 tie-breaker가 **거의 언제나** 걸리는 정렬이다(같은 동 = 같은 거리).
  0011이 `(정렬값, id)`로 만들어 둔 것이 여기서 값을 했다. 27건 3페이지 중복·누락 0으로 확인.
  커서가 부동소수인 것도 처음이라, Postgres float8 출력과 JS `String()`이 둘 다 최단 왕복
  표기라 비트가 보존된다는 것을 확인하고 썼다.
- **지도**(`0025`, 2026-08-06): `/search/map`. **마커 하나가 동네 하나**다 — 같은 이유로,
  글마다 핀을 찍으면 한 점에 겹쳐 쌓이고 흩뿌리면 없는 위치를 지어낸다.
  (`posts.trade_location`은 실제 POI라 겹치지 않지만 장소를 고른 글에만 있어, 그것만 찍으면
  나머지 글이 지도에서 사라진다. 뜻이 다른 마커를 섞지 않기로 했다.)
- **지도에서도 데이터가 설계를 바꿨다**: 같은 법정동인데 **이름 표기와 좌표가 갈린다.**
  `coord2RegionCode`는 "서울특별시 …", `addressSearch`는 "서울 …"을 주고, 좌표는 후자가
  **그 지번의 값**을 준다(`fromAddressSearchResult`). 셋으로 묶으면 한 동네가 마커 여럿으로
  쪼개져, **코드로만 묶고** 이름은 최빈값·좌표는 평균을 쓴다.
- **딸려 나온 것 하나 더**: `toSearchRadiusLabel`이 잰 거리에서 "5.0km"를 내던 것을 고쳐
  `shared/utils/formatDistance`로 올렸다 — 고른 값(500·1000·2000)만 다루던 함수에 잰 값이
  들어온 자리다.

### 5-2. 가격 제안 취소 — ✅ 완료 (`0026` · `0027`, 2026-08-06)

- **정한 것: 답변 대기 중인 제안만 무를 수 있다.** `accepted`·`rejected`는 상대가 이미 답한
  것이라 한쪽이 혼자 되돌리면 합의가 깨진다. 말풍선은 지우지 않고 "취소됨"으로 남는다 —
  0008이 "대화 기록이 사후에 바뀌면 채팅을 신뢰할 수 없다"고 정한 자리다.
- **문제는 혼자서는 안 보이고 둘이 겹쳐야 보였다**: `messages_update`가 발신자를 빼 둔 것(0008)과
  `hasPendingOfferFrom`이 대기 중이면 새 제안을 막는 것이 겹치면, 잘못 보낸 사람이 무를 수도
  새로 보낼 수도 없다. 각자는 옳은 판단이었다.
- **파 보니 나온 구멍**: 0008의 guard가 `offer_status`를 통째로 열어 둬서 **받는 쪽이
  `accepted`를 `rejected`로 뒤집을 수 있었다.** 화면이 `.eq('offer_status','pending')`으로
  막고 있어 **버그로 보이지 않았다**(0023의 `manner_temp`와 같은 모양). 함께 닫았다.
- **enum 값 때문에 마이그레이션이 둘로 갈렸다**: `alter type ... add value`로 더한 값은
  **같은 트랜잭션에서 쓸 수 없다**(직접 밟아 확인). 0026이 값만, 0027이 규칙을 얹는다.
- **딸려 나온 수정**: 트리거 문구를 늘렸더니 `chatErrorMessage`의 `/읽음 표시만/` 패턴이
  안 걸리게 됐다 — 오류가 아니라 **조용히 기본 문구로 떨어지는** 종류라 눈에 안 띈다.
  서버 문구를 그대로 넣는 테스트를 붙였다.
- 막혔던 자리는 `troble.md`의 "롤백 테스트가 '통과'라고 거짓말한다".

> **착수 전에 적어 둔 예상** — 여기서는 대체로 맞았다. "정책을 '발신자는 `offer_status`를
> `cancelled`로만' 쪽으로 손보고 enum 값을 추가"가 실제 모양과 거의 같다. 다만 **정책 하나로는
> 안 됐다** — 정책은 어느 칸이 바뀌었는지 못 보므로 값을 재는 일은 트리거로 갔고,
> enum 값 때문에 파일이 둘로 갈리는 것도 예상에 없었다.

### 5-3. "인기순"의 정의 — ⏸ 데이터가 쌓인 뒤로 미룸 (2026-08-06)

- **정한 것: 지금은 그대로 둔다.** 라벨이 이미 "조회 많은 순"이라 거짓말을 하고 있지 않다.
- **미룬 이유는 데이터다.** 착수 전에 실제 값을 봤더니 **조회 최대 3, 찜 최대 1, 채팅 최대 1**로
  사실상 전부 0이었다. 가중치를 무엇으로 정하든 **화면에서 차이가 보이지 않는다** —
  기계적 동작은 검증할 수 있지만 "이 가중치가 맞는가"는 확인할 방법이 없다.
  §5-8(매너 태그 통계)을 미룬 것과 같은 취급이다.
- **열 때의 모양은 정해 뒀다**: `view_count`·`like_count`는 이미 `posts`에 있으므로
  **생성 컬럼 하나 + 인덱스**면 끝나고, 기존 정렬·keyset 기계는 그대로 쓴다
  (`v_column`을 그 컬럼으로 가리키기만 하면 된다). 백필도 필요 없다.
  **채팅까지 넣으려면** `chat_count` 컬럼·`chat_rooms` 트리거·백필이 붙는다(0005의 `like_count` 방식).
  가중치를 바꾸는 것도 생성 컬럼을 다시 만드는 마이그레이션 하나다.

### 5-4. 알림 설정 (종류별 on/off) — ✅ 완료 (`0022` · `0023`, 2026-08-05)

- **정한 것: 타입별. 그중에서도 댓글·관심(찜)·거래후기 셋만 끌 수 있다.**
  채팅·가격 제안은 못 끈다 — 끄면 상대는 답을 기다리는데 나는 모르는 상태가 되고,
  그 **피해가 나에게 오지 않고 거래 상대에게 간다.**
- 그 판단을 스키마에 박았다. `jsonb` 한 칸 대신 **칸 셋**이다 — 다섯 키를 넣어 두면
  "chat도 넣을 수 있는데 왜 안 넣지"가 되고, 끌 수 없다는 사실이 코드 어딘가의 `if`로만 남는다.
- **끈 알림은 아예 만들지 않는다.** 0015가 적어 둔 "목록만 걸러 내면 배지 숫자와 어긋난다"
  그대로다. 후기만 모양이 다르다 — 매너온도 계산은 이 설정과 무관하게 언제나 돈다.
- **딸려 나온 구멍** (`0023_profile_guard.sql`): `profiles_update`가 `using (auth.uid() = id)`
  하나뿐이라 **사용자가 자기 `manner_temp`를 99로 직접 쓸 수 있었다**(실제로 밟아 확인).
  0013이 후기에 들인 공이 이 한 줄로 전부 비켜 갔다. `current_user`로 경로를 갈라 막았다 —
  시스템(`sync_manner_temp`, definer)은 그대로 통과한다.
- **남은 것**: 글별 알림 끄기(`muted_posts` 테이블이 따로 붙는다).
  푸시(§5-6)가 붙으면 "앱 안에서는 받고 푸시는 안 받기"가 한 겹 더 얹힐 수 있다.

### 5-5. 비밀번호 재설정 — ✅ 코드 완료 (2026-08-07) · ⏳ 대시보드 셋 남음

**Confirm email을 다시 켜면서 열렸다**(2026-08-07). 마이그레이션은 없다 — 전부 화면과 인증이다.

- `/forgot-password`(메일 보내기) · `/reset-password`(새 비밀번호) · 로그인 화면의 링크.
- **착지 지점을 `/auth/callback`과 갈랐다.** 그쪽은 세션이 서면 곧바로 홈으로 보내는 화면이라,
  거기로 보내면 **로그인만 되고 비밀번호를 정할 자리 없이 밀려난다.**
- **가입 여부를 알려 주지 않는다.** "그런 이메일은 없습니다"를 내면 주소를 넣어 보며 누가
  이 서비스를 쓰는지 알아낼 수 있다 — 동네가 붙어 다니는 서비스라 가볍지 않다.
- **"로그인돼 있으면 통과"로 두지 않았다.** 그러면 남이 열어 둔 브라우저로 주소만 쳐서
  비밀번호를 바꿔 계정을 가져갈 수 있다(`accountApi.changePassword`가 현재 비밀번호를 다시
  묻는 이유와 같은 자리). `PASSWORD_RECOVERY` 사건을 스토어에 기억해 막는다.
- **파 보고 안 것**: supabase-js는 초기화 중의 알림을 큐에 담았다가 **나중에 등록된 구독자에게는
  안 준다.** `useAuthSessionSync`가 세션을 먼저 읽고 있어 그 사건을 놓칠 자리였다 —
  구독을 앞으로 옮겼다(`troble.md`).
- **남은 것은 대시보드 셋이다. 코드가 못 한다.**
  1. **커스텀 SMTP** — 내장 SMTP는 한도가 낮아 메일 자체가 안 나간다
  2. **Redirect URLs에 `/reset-password`** — 없으면 링크가 Site URL로 떨어진다
  3. **Secure password change** — `updateUser({password})`에 최근 로그인을 요구한다.
     위의 화면 쪽 확인은 **UX이지 보안 경계가 아니다** — 서버 잠금은 이 스위치뿐이다
- **메일이 오가는 것은 못 밟았다.** 확인 순서는 `note.md`에 적어 두었다.

### 5-6. 푸시 알림

- 앱이 꺼져 있을 때 오는 알림. 서비스 워커 + VAPID 키 + 구독 저장이 필요해 **이 단계와 무게가 다르다.**
  하나의 PR로 잡을 크기인지부터 판단해야 한다.

### 5-7. 신고 처리 · 자동 제재

- `reports`는 select 정책이 없어 **설계상 관리자 전용**이다. 관리자 도구는 이 앱 밖의 일이라고
  6단계에서 정리했다 — 그 판단을 유지할지가 먼저다.
- "신고가 쌓이면 자동으로 가린다"는 규칙은 **기준(몇 건? 어떤 사유?)을 사람이 정해야** 한다.

### 5-8. 매너 태그 통계

- 만드는 법은 정해져 있다(`unnest` + `group by`). 다만 **후기가 쌓이기 전에는 볼 것이 없어**
  지금 붙여도 빈 화면이다. 데이터가 모인 뒤에 판단한다.

### 5-9. 비밀 댓글 — ✅ 완료 (`0033_secret_comment.sql`, 2026-08-07)

- **정한 것 셋**: 판매자 + **실타래를 연 사람**이 본다 · 답글은 **부모를 따라간다**(고를 수 없다) ·
  남에게는 **완전히 감춘다**("비밀 댓글입니다" 자리도 안 남긴다).
- **"작성자"가 아니라 "실타래를 연 사람"이다.** 비밀 댓글에 판매자가 답하면 그 답글의
  `author_id`는 판매자이고 물어본 사람은 그 행에 없다 — 그대로 적었으면 **자기가 받은 답을
  못 보게 된다.**
- **여기 적어 둔 "공개 범위가 얹힌다"는 맞았다.** 다만 얹는 자리가 정책만이 아니었다 —
  답글이 부모를 따르게 하려면 **정책 안에서 부모 댓글을 읽어야 하는데, 정책은 자기 테이블을
  다시 읽을 수 없다**(`42P17 infinite recursion`, 직접 밟아 확인). `private`의 definer 함수로
  뺐다. 0014·0031에 이어 세 번째 같은 자리다 — 자세한 것은 `troble.md`.
- **답글 강제는 정책이 아니라 트리거다.** 정책은 막을 수만 있고 고쳐 넣을 수는 없어서,
  막는 쪽으로 만들면 화면이 부모 값을 정확히 실어 보내야 한다(규칙을 아는 곳이 둘이 된다).
  받아 적으면 화면이 몰라도 틀릴 수가 없고, 틀리더라도 **감추는 쪽으로만** 틀린다.
- **카드의 "댓글 n"은 공개 댓글만 센다**(0028의 트리거에 줄 둘). `comment_count`는 평범한
  컬럼이라 RLS가 안 걸려 **보는 사람마다 다를 수 없다** — 그러면 기준은 가장 적게 보는
  사람이어야 한다. 전부 세면 그 차이가 곧 "여기 비밀 댓글이 있다"는 신호가 된다.
- **알림은 한 줄도 안 고쳤다.** 0018이 알리는 상대(판매자 · 부모 댓글 작성자)가 **곧 볼 수
  있는 사람**이라 조건을 더할 자리가 없었다. 우연이 아니라 같은 규칙에서 나온다.
- **화면은 아직 눈으로 못 봤다.**
- **새로 미룬 것**: 비밀 댓글의 알림 문구 구분(`fetch_notifications`에 칸이 붙는다) ·
  판매자가 자기 글에 다는 비밀 댓글(막지 않았다).

---

## 6. 이미 해소된 옛 "범위 밖" (참고)

앞 단계 노트에 "범위 밖"으로 적혀 있지만 **지금은 있는** 것들이다. 옛 문서를 읽다가 착각하지
않도록 남긴다.

게시물 수정·삭제·상태 변경(2단계) · 찜 목록 화면 `/my/likes`(마이페이지) · 최근 본 상품
`/my/recent` · 검색·카테고리 필터(3단계) · 정렬 옵션(3단계) · 홈 무한 스크롤(3단계) ·
가격 제안 보내기·수락·거절(4단계) · 거래후기·매너온도(5단계) · 다른 사용자 프로필(5단계) ·
차단·신고(6단계) · 알림 화면(7단계) · 하단 탭바·다크모드(1단계) ·
**비밀번호 변경·회원탈퇴(8-1)** · **후기 삭제 시 매너온도(0016)** ·
**게시물 댓글 1단(0017)** · **댓글·찜 알림(0018)** · **대댓글** ·
**알림 개별 삭제(0019)** · **"수정됨" 표시·댓글 수정(0020)** · **첫 거래 안내(0021)** ·
**알림 종류별 on/off(0022·0023)** · **반경 검색·거리순·검색 반경 설정(0024)** ·
**지도 — 동네 단위 마커(0025)** · **가격 제안 취소(0026·0027)** ·
**게시물 카드의 댓글 수(0028)** · **탭바 밖 헤더 통일** ·
**메시지 삭제(0029)** · **채팅방 나가기(0030)** · **양쪽 나간 방 완전 삭제(0031)** ·
**게시물 삭제의 뒷정리(0032)** · **비밀 댓글(0033)** · **매너온도 이력(0034)** ·
**거래 상대 판정을 트리거로(0035)** · **비밀번호 재설정(§5-5, 코드 쪽)**

---

## 7. 착수 순서 제안

1. ~~8-1 계정~~ — ✅ 끝났다(PR #18). 딸려 나온 매너온도 구멍도 `0016`으로 닫았다(PR #19).
2. ~~댓글 1단(§2-1)~~ — ✅ `0017`, PR #20. **대댓글이 남았다.**
3. ~~댓글·찜 알림 트리거(§3-2)~~ — ✅ `0018`, PR #22.
4. ~~대댓글~~ — ✅ 2026-08-05. 마이그레이션 없이 화면만으로 끝났다. 미리 만들어 둔 것
   (`parent_id`·인덱스·정책·알림)이 실제로 값을 절약한 경우다.
5. ~~**§4의 짧은 것들**~~ — ✅ 셋 다 끝났다. 알림 개별 삭제(`0019`) ·
   "수정됨" 표시 + 댓글 수정(`0020`) · 첫 거래 안내(`0021`).
   **§4에 남은 것은 판단이 끝난 자잘한 것들뿐이다** — 끌올 시간 실시간 갱신, 메시지 삭제·
   채팅방 나가기, 후기 수정, 매너온도 이력, FAB·헤더 통일, 댓글 관련 넷.
6. ~~**§5는 결정부터**~~ — 5-4(알림 on/off)는 `0022`·`0023`, **5-1의 앞쪽 절반(반경 검색 병행)은
   `0024`로 끝났다**(2026-08-06). §3-1의 자산 셋 중 둘이 살아났고 하나는 지웠다.
7. ~~**지도 화면**~~ — ✅ `0025`, 2026-08-06. **§2-2와 §3-1이 함께 닫혔다.**
   `libraries` 옵션은 손댈 것이 없었고(§3-1), 대신 0024의 WHERE를 지도와 공유하는 일이 생겼다.
8. ~~**5-2(가격 제안 취소)**~~ — ✅ `0026`·`0027`, 2026-08-06. 예상보다 한 겹 더 있었다 —
   enum 값 때문에 파일이 둘로 갈렸고, 0008이 열어 둔 전이 구멍도 함께 닫았다.
9. ~~**5-3(인기순 정의)**~~ — ⏸ **데이터가 쌓인 뒤로 미뤘다**(2026-08-06). 실제 값이 조회 3·찜 1·
   채팅 1이라 가중치를 무엇으로 정하든 화면에서 차이가 안 보인다. 열 때의 모양은 적어 뒀다.
10. ~~**§4의 자잘한 것 둘**~~ — ✅ 끌올 시간 실시간 갱신 · 글쓰기 FAB (2026-08-06, 마이그레이션 없음).
11. ~~**게시물 카드의 댓글 수**~~ — ✅ `0028`, 2026-08-06. 다시 잰 비용(RPC 여섯 반복)이 맞았다.
12. ~~**채팅방·상세 헤더 통일**~~ — ✅ 2026-08-06. 열 곳이었고 한 틀로 덮였다.
13. ~~**메시지 삭제 · 채팅방 나가기**~~ — ✅ `0029`·`0030`, 2026-08-06. 예상대로 칸이 붙었지만
    §5로 넘어갈 만큼은 아니었다. **딸려 나온 것이 하나 더 있었다** — `with check` 없는 update
    정책이 삭제를 스스로 막는 자리(`troble.md`).
14. ~~**채팅방 완전 삭제**~~ — ✅ `0031`, 2026-08-06. 버튼이 아니라 **양쪽이 다 나갔을 때
    조용히 지우는 쪽**으로 갔다. 파 보니 방을 지우면 `posts_update`가 잠기고, 알림에는
    cascade가 없고, 사진은 DB가 못 지운다 — 셋 다 이번에 닫거나 피했다.
15. ~~**5-9(비밀 댓글)**~~ — ✅ `0033`, 2026-08-07. "누구에게 보이는가"를 새로 정하는 일 중
    첫 번째를 닫았다. **예상에 없던 것은 재귀였다** — 정책이 자기 테이블을 못 읽어
    definer 함수가 하나 더 붙었고(0014·0031에 이어 셋째), 대신 **알림은 손댈 곳이 아예 없었다.**
16. ~~**매너온도 이력**~~ — ✅ `0034`, 2026-08-07. **미루면 미룬 만큼 영영 비는** 항목이라
    §4의 나머지보다 먼저 갔다. 정할 것("무엇을 남길지")은 **이유 대신 근거**로 답했다 —
    그래야 어긋남 자체를 잡는다. 밟다가 `grant`가 아무것도 안 좁히고 있었다는 것도 알았다
    (`troble.md`).
17. ~~**거래 상대 판정을 트리거로**~~ — ✅ `0035`, 2026-08-07. 0031의 부채를 갚으러 갔다가
    **정책이 update에만 걸려 있어 지어낸 거래로 남의 매너온도를 깎을 수 있다는 것**을 찾았다.
    옮기는 일보다 그쪽이 컸다. 남는 규칙: **한 규칙이 두 명령에 걸려야 하면 정책은 잘못된
    도구다**(`troble.md`).
18. ~~**비밀번호 재설정(§5-5)**~~ — ✅ 2026-08-07, 마이그레이션 없음. Confirm email이 켜지면서
    열렸다. **코드는 끝났고 대시보드 셋이 남았다**(커스텀 SMTP · Redirect URLs · Secure
    password change). 파 보니 supabase-js가 초기화 중의 인증 사건을 늦게 구독한 쪽에는
    안 준다는 것도 알았다(`troble.md`).
19. **여기가 지금 자리다.** 무게중심이 **"만들기"에서 "배포 준비 + 확인"으로 넘어갔다.**
    명세(`feature.md`) 대비 남은 구멍은 **카카오 로그인(§1-2)** 하나인데 콘솔 준비가 사람 손을 탄다.
    **§4에서 "손만 대면 되는 것"은 다 떨어졌다** — 남은 것은 전부 판단이 하나씩 남아 있다.
    - **화면 확인이 다섯 건째 밀려 있다** — FAB · 헤더 통일 · 메시지 삭제/나가기 · 비밀 댓글 ·
      매너온도 기록. 전부 "눈으로 못 봤다"로 적혀 있다. **다음 기능보다 이쪽이 먼저일 수 있다.**
      저장소에 브라우저 자동화 도구가 없어(playwright·puppeteer·cypress 없음) 사람이 봐야 한다.
    - **5-7(신고 처리)** — "누구에게 보이는가"를 정하는 일이 하나 남았다. 다만 그 전에
      "관리자 도구는 이 앱 밖"이라는 6단계의 판단을 유지할지부터다.
    - **§4의 채팅·댓글 잔여** — 끊긴 뒷정리 다시 시도 · 알림 "모두 삭제" ·
      댓글 페이징 · 댓글 알림 묶기.
    - ~~후기 수정~~ — **여기 후보로 적어 두었으나 §4에서 이미 "안 열기로" 닫힌 항목이다**
      (2026-08-06, 재검토 대상 아님). 두 절이 어긋나 있었다.
    §5의 나머지(5-5 비밀번호 재설정 · 5-6 푸시)는 배포 준비(커스텀 SMTP)와 묶여 있고,
    §5-3(인기순)·§5-8(매너 태그 통계)은 데이터가 쌓인 뒤다.

카카오 로그인(§1-2)은 순서에서 뺐다 — 콘솔 준비가 사람 손을 타서 언제든 그 준비가 된 때
끼워 넣으면 된다.

---

## 작업 규칙 (`SKILL.md` — 옮겨 적음)

- 기능마다 `develop`으로 PR (`feature : 한글 요약`). `main` push 금지.
- 구현 후 `note.md`에 구현 노트, 막혔던 부분은 `troble.md`에 "증상 → 원인 → 해결".
- 화살표 함수 금지 / `any` 금지 / 파일명 camelCase / 상수 UPPERCASE — `eslint.config.js`가 강제한다.
- 기능마다 Jest 테스트를 붙이고 통과시킨 뒤 적용한다.
