/**
 * 가지 표식의 도형 하나.
 *
 * `brandMark.tsx`(화면)와 `public/favicon.svg`(탭)가 **같은 가지를 그려야** 한다.
 * 두 벌로 두면 한쪽만 고쳐지는 날이 오고, 그때부터 화면과 탭이 다른 가지가 된다.
 * 그래서 좌표와 색을 여기 한 곳에 두고, 파비콘이 이 값과 같은지는
 * `brandMarkShape.test.ts`가 지킨다.
 *
 * 파비콘은 SVG 파일이라 이 상수를 import할 수 없다(빌드가 손대지 않는 정적 파일이다).
 * 그래서 **값을 옮겨 적되, 어긋나면 테스트가 깨지도록** 했다.
 */

/** 24 × 24. 열매는 y 6.2~21, 꼭지와 줄기가 y 2.9까지 올라가 뷰박스를 거의 채운다. */
export const BRAND_MARK_VIEW_BOX = '0 0 24 24';

/**
 * 파비콘용 뷰박스. **다른 그림이 아니라 같은 그림을 바짝 잘라 본 것이다.**
 *
 * 표식은 가로로 x 4.9~19.1만 쓴다 — 24 중 14.2다. 화면에서는 옆 글자와의 여백이 되어
 * 좋지만, 탭에서는 16px 중 몇 픽셀을 그냥 버리는 셈이다. 그래서 도형이 실제로 차지하는
 * 네모(x 4.9~19.1, y 1.7~21 · 줄기 둥근 끝까지 친 값)를 정사각형으로 맞춰 잡았다.
 * 경로는 그대로라 실루엣은 같다.
 */
export const BRAND_MARK_VIEW_BOX_FAVICON = '1.8 1.15 20.4 20.4';

export const BRAND_FRUIT_COLOR = '#7C3AED';
export const BRAND_LEAF_COLOR = '#16A34A';

/** 열매. 위가 좁고 아래가 부푼 모양이라야 가지로 읽힌다. */
export const BRAND_FRUIT_PATH =
  'M12 6.2c4.1 0 7.1 3.4 7.1 7.7 0 4.2-3.2 7.1-7.1 7.1s-7.1-2.9-7.1-7.1c0-4.3 3-7.7 7.1-7.7z';

/** 꼭지. 잎 두 장. */
export const BRAND_LEAF_PATH =
  'M12 7.4c-2.1 0-3.8-1.1-4.6-2.8 1.9-.7 3.6-.2 4.6 1 1-1.2 2.7-1.7 4.6-1-.8 1.7-2.5 2.8-4.6 2.8z';

/** 줄기. 채우기가 아니라 선이라 굵기가 따로 필요하다. */
export const BRAND_STEM_PATH = 'M12 5.6V2.9';

/** 화면용 줄기 굵기. 24px 이상에서 쓴다. */
export const BRAND_STEM_WIDTH = 1.6;

/**
 * 파비콘용 줄기 굵기.
 *
 * 탭 아이콘은 16px까지 줄어든다. 그때 1.6은 24분의 1.6 → **한 픽셀 남짓**이라
 * 안티에일리어싱에 먹혀 줄기가 사라진다. 굵기만 키운다 —
 * **실루엣(경로)은 그대로 두어야 같은 가지로 보인다.**
 */
export const BRAND_STEM_WIDTH_FAVICON = 2.4;
