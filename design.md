# 디자인 — 반응형 규칙

가지마켓 화면이 폭에 따라 어떻게 달라지는지, **왜 그렇게 정했는지** 적어 둔다.
새 화면을 만들 때는 여기 규칙을 먼저 읽고, 규칙에 없는 것을 정했으면 여기에 더한다.

---

## 0. 전제 — 모바일이 기본이다

이 앱은 중고거래다. 물건을 올리는 곳도 보는 곳도 손에 든 화면이라, **모바일이 예외가 아니라
기본**이다. 그래서 반응형을 이렇게 방향 잡았다.

- 기본 클래스는 **모바일 그대로 둔다.** `md:`·`lg:`를 **더해서** 데스크탑을 만든다.
- 뺄셈으로 만들지 않는다 — 데스크탑을 기본으로 두고 `max-md:`로 줄이면, 다음 사람이
  모바일 화면을 고칠 때 **어느 값이 진짜 기본인지** 매번 되짚어야 한다.
- **`md` 아래에서는 이번 작업 전과 픽셀 하나 다르지 않다.** 갈라지는 것은 768px부터다.

브레이크포인트는 Tailwind 기본값을 그대로 쓴다.

| 이름 | 폭 | 이 앱에서 뜻하는 것 |
|---|---|---|
| (기본) | ~767px | 손에 든 화면. 한 손, 엄지, 세로 스크롤 |
| `md` | 768px~ | 태블릿·좁은 데스크탑. **길잡이가 위로 올라가는 지점** |
| `lg` | 1024px~ | 데스크탑. **둘을 나란히 놓을 수 있는 지점** |
| `xl` | 1280px~ | 넓은 데스크탑. 격자가 한 열 더 늘어난다 |

---

## 1. 폭 — 화면은 픽셀이 아니라 **성격**을 적는다

### 문제

반응형을 붙이기 전, `max-w-screen-sm`이 **스물세 파일**에 그대로 적혀 있었다.
그 상태로 데스크탑 폭을 더하면 `md:max-w-4xl`이 스물세 번 복사되고, 다음에 값을 바꿀 때
또 스물세 곳을 고쳐야 한다. **한 곳을 빠뜨리면 그 화면만 좁게 남는데, 그건 오류가 아니라
"조금 이상한 화면"이라 눈에 안 띈다.**

### 정한 것

`src/styles/index.css`에 유틸리티 셋을 두고, 화면은 **폭이 아니라 성격**을 적는다.
바탕색을 거기서 한 번만 정하는 것과 같은 이유다.

```css
@utility page-narrow { @apply mx-auto w-full max-w-screen-sm md:max-w-2xl; }
@utility page-wide   { @apply mx-auto w-full max-w-screen-sm md:max-w-4xl lg:max-w-6xl; }
@utility page-detail { @apply mx-auto w-full max-w-screen-sm lg:max-w-5xl; }
```

| 유틸리티 | 쓰는 곳 | 고른 이유 |
|---|---|---|
| `page-narrow` | 폼 · 설정 · 온보딩 · 후기 쓰기 · **채팅방** | 한 줄이 길어지면 못 읽는 것들. 글줄이 화면 끝까지 가면 다음 줄 첫 글자를 눈이 못 찾는다 |
| `page-wide` | 홈 · 검색 · 지도 · 알림 · 마이페이지 목록 · 남의 프로필 | 많이 늘어놓을수록 나은 것들. 목록은 한 번에 보이는 개수가 곧 쓸모다 |
| `page-detail` | 게시물 상세 | 2단으로 갈라 놓을 화면. 넓히되 `page-wide`만큼은 아니다 |

**채팅방이 `page-narrow`인 것이 이 표에서 유일하게 헷갈리는 자리다.** 목록처럼 보이지만
말풍선은 글이라, 1200px로 벌리면 한 줄에 100자가 넘어가 대화로 안 읽힌다.

### 새 화면을 만들 때

셋 중 하나를 고른다. **`max-w-*`를 직접 적지 않는다.** 셋으로 안 되는 화면이 나오면
그때 넷째를 여기 더하고 이 표에 한 줄을 쓴다.

---

## 2. 길잡이 — 폭에 따라 자리를 옮긴다

### 정한 것

같은 `APP_TABS` 다섯 자리를 **두 모양으로** 그린다. 서로가 서로를 감춘다.

| | 모바일 (~767px) | 데스크탑 (768px~) |
|---|---|---|
| 어디에 | 화면 **아래** 고정 (`AppTabBar`) | 화면 **위** 고정 (`AppHeaderNav`) |
| 감추기 | — | `md:hidden` |
| 나오기 | — | `hidden md:block` |
| 모양 | 아이콘 + 작은 글자, 다섯 칸 균등 | 로고 + 글자 링크 넷 + 오른쪽에 알림·글쓰기 |
| 글쓰기 | 떠 있는 **원** (탭바 위로 반쯤) | 채워진 **버튼** |

### 왜 아래를 그대로 두지 않는가

**데스크탑에서 화면 아래는 눈에서 가장 먼 곳이다.** 하단 탭바는 엄지가 닿는 곳에 만든
물건이라, 그대로 올려 두면 마우스가 무언가 할 때마다 화면 끝까지 내려가야 한다.
모니터에서 시선이 처음 닿는 곳은 위쪽 왼편이고, 웹에서 길잡이가 거기 있는 것은
사람들이 이미 아는 규칙이다.

### 왜 아이콘을 뺐는가

데스크탑에서는 글자가 들어갈 폭이 충분하다. 그때 아이콘은 **같은 말을 두 번** 하는 셈이고,
이모지는 키우면 거칠어진다. 모바일에서는 반대다 — 다섯 칸에 글자만 넣으면 읽을 수 없어
아이콘이 실제로 일을 한다.

### 글쓰기가 두 모양인 것

`isPrimary` **값 하나**가 두 모양을 만든다. "다섯 중 이것만 성격이 다르다(보러 오는 곳이
아니라 하러 오는 곳)"는 사실은 하나고, 그 사실을 어떻게 그리느냐가 화면마다 다를 뿐이다.
값이 하나라서 자리를 더하거나 순서를 바꿔도 엉뚱한 탭이 튀어나오지 않는다.

### 딸려 나온 것 — 같은 것이 두 번 보이지 않게

데스크탑 헤더가 로고와 알림 종을 들면서 홈 화면과 겹쳤다. 겹치는 쪽을 감춘다.

- 홈의 `🍆 가지마켓` 제목 → `md:hidden` (헤더가 이미 들고 있다)
- 홈의 알림 종 → `md:hidden` (**배지가 한 화면에 둘이면 어느 쪽이 맞는지 알 수 없다**)

알림 종이 헤더로 올라간 덕에 **데스크탑에서는 어느 화면에서든 알림으로 갈 수 있다.**
모바일은 지금처럼 홈에만 있다 — 탭바가 다섯 칸으로 이미 좁다.

---

## 3. 목록 — 줄에서 격자로

### 정한 것

`PostList`(홈·검색)와 `MyPostList`(마이페이지 넷)가 **같은 규칙**을 쓴다.

```
모바일   flex flex-col divide-y        한 줄에 하나, 가로선으로 나눔
md       grid-cols-2 divide-y-0       두 칸
xl       grid-cols-3                  세 칸
```

`divide-y`는 줄 목록일 때만 뜻이 있어 격자에서 걷어낸다(`md:divide-y-0`) —
안 걷어내면 칸 사이에 가로선이 어긋나게 그어진다.

### 열 수를 2·3으로 정한 이유

`page-wide`가 `lg`에서 1152px까지 벌어진다. 거기서 4열로 나누면 카드가 260px 밑으로
좁아져 **제목이 대부분 잘린다.** 격자는 "몇 개가 들어가나"가 아니라 **한 칸이 읽히나**로
정한다.

### 무한스크롤 표식

마지막의 관찰용 `<li>`가 격자에서 한 칸을 차지하면 **빈 칸이 하나 생겨 마지막 줄이
어긋난다.** `md:col-span-full`로 한 줄을 통째로 쓰게 두면 높이 1px짜리 띠로만 남는다.

---

## 4. 게시물 카드 — 한 줄에서 타일로

`PostCard` 하나가 두 모양이다. 카드가 둘인 것이 아니다.

| | 모바일 | 데스크탑 (`md`~) |
|---|---|---|
| 방향 | 가로 (`flex`) | 세로 (`md:flex-col`) |
| 사진 | 96×96 왼쪽 | 정사각형, **칸 전체 폭** (`md:aspect-square md:w-full`) |
| 호버 | 배경색 | 없음 (`md:hover:bg-transparent`) — 타일에서는 사진이 이미 경계다 |

사진에 높이를 고정하지 않고 `aspect-square`로 둔 것은 **격자 열 수가 바뀌어도 비율이
그대로**이기 때문이다.

**이 카드를 목록 여섯이 함께 쓴다**(홈·검색·찜·최근 본 글·구매내역·판매관리).
`PostCard`가 처음부터 "같은 게시물이 화면마다 다르게 보일 이유가 없다"고 적어 둔 덕에,
여기 한 곳을 고치니 여섯이 함께 따라왔다.

---

## 5. 게시물 상세 — `lg`부터 2단

### 문제

모바일에서는 사진 → 판매자 → 제목·가격 → 설명 → 버튼을 위에서 아래로 훑는다.
그 순서를 데스크탑에 그대로 세우면 **사진이 화면을 가득 채우고 가격과 버튼이 스크롤
아래로 밀린다** — 살지 말지 정하는 데 필요한 것이 한눈에 안 들어온다.

### 정한 것

```
lg:grid lg:grid-cols-2 lg:items-start lg:gap-8

왼쪽   사진 (lg:sticky lg:top-16)
오른쪽 판매자 · 제목 · 가격 · 설명 · 거래장소 · 버튼
아래   댓글 (2단 밖, lg:max-w-2xl)
```

- **`lg`부터**인 것은 `md`(768px)에서 반으로 가르면 사진도 글도 둘 다 좁아서다.
- `items-start`가 없으면 짧은 쪽이 긴 쪽 높이만큼 늘어난다.
- `lg:sticky lg:top-16` — 설명이 길어도 사진이 따라온다. `top-16`은 상단 내비게이션
  밑에 걸리지 않게 하는 값이다.
- 제목·가격은 `lg:text-2xl`로 키운다. 넓은 화면에서 같은 크기면 정보의 무게가 안 보인다.

### 댓글이 2단 밖인 이유

댓글은 물건을 다 본 뒤에 읽는 **대화**다. 오른쪽 칸에 끼워 넣으면 가격·버튼과 자리를
다투고 왼쪽 사진과 높이가 어긋난다. 대신 `lg:max-w-2xl`로 폭을 좁힌다 —
1단 규칙(§1의 `page-narrow`)이 여기서도 같은 이유로 걸린다.

---

## 6. 아이콘 — 이모지에서 SVG로

### 왜 바꿨나

화면의 아이콘이 전부 **이모지 문자**였다. 이모지는 폰트가 그리는 글자라 세 가지가 걸린다.

1. **기기마다 모양이 다르다.** 같은 `🏠`가 애플·안드로이드·윈도우에서 다른 집이고,
   후기의 `😐`는 애플에서 무표정, 안드로이드에서는 뾰로통하다 — **남의 평가를 옮겨 적는
   자리라 그 차이가 뜻을 바꾼다.**
2. **색을 정할 수 없다.** 탭바에서 켜진 탭과 꺼진 탭의 글자색은 갈랐는데 **아이콘은 늘
   같은 색**이었다. 이모지는 자기 색을 들고 있어 `text-emerald-600`이 안 먹는다.
3. **키우면 거칠어진다.** 데스크탑 상단 바에 아이콘을 못 넣은 이유가 이것이었다.

### 정한 것 — `lucide-react`

```
lucide-react ^1.29.0   (+8.5 kB raw / +3 kB gzip)
```

- **`currentColor`로 그리는 SVG다.** 글자색을 그대로 따라가므로 활성 색을 아이콘에 따로
  적을 것이 없다 — 위 2번이 그대로 풀린다.
- 이름 있는 import만 쓰면 **쓰는 아이콘만 번들에 들어간다.**
- 스트로크 하나로 그린 한 벌이라 굵기·여백이 이미 맞춰져 있다.

### 크기 규칙

같은 아이콘이라도 자리에 따라 크기가 다르다. `size` prop으로 적는다(클래스로 안 한다 —
lucide가 `width`/`height` 속성으로 받는다).

| 자리 | 크기 |
|---|---|
| 하단 탭바 | 20 (글쓰기 원 안은 22) |
| 상단 내비게이션 | 16 |
| 목록 줄 앞 (마이페이지 메뉴) | 18 |
| 버튼 안 (찜·현재 위치·후기) | 16 |
| 꺾쇠(`ChevronRight`) | 14~16 |
| 기본 프로필 이미지 | 원 크기를 따라 16 / 22 / 40 |

### 고른 아이콘

| 자리 | 이모지 → lucide | 고른 이유 |
|---|---|---|
| 홈 · 검색 · 채팅 · 나 | 🏠🔍💬👤 → `Home` `Search` `MessageCircle` `User` | 그대로 대응된다 |
| 글쓰기 | ✏️ → `PenLine` | `Plus`도 후보였는데 "무엇이든 더한다"는 뜻이라 **파는 글을 쓰러 가는** 자리와 어긋난다 |
| 알림 / 알림 설정 | 🔔 / 🔕 → `Bell` / `BellOff` | 마이페이지에서 둘이 나란히 있어 종 하나로는 안 갈린다 |
| 관심목록 · 찜 버튼 | ♡♥ → `Heart` | 채움만 `fill`로 가른다. 예전에는 `♥`와 `♡`가 **서로 다른 글자**라 눌렀을 때 굵기가 달라져 아이콘이 움찔했다 |
| 후기 평가 | 😊😐😞 → `Smile` `Meh` `Frown` | 위 1번의 표정 문제 |
| 뒤로 · 꺾쇠 · 닫기 | `←` `›` `‹` `✕` → `ArrowLeft` `ChevronRight` `ChevronLeft` `X` | 글꼴 글자라 줄 높이에 따라 위아래로 흔들렸다 |
| ⋯ 메뉴 | `⋯` → `MoreHorizontal` | 같은 이유 |

### 이모지를 그대로 둔 자리

- **`·` 구분점, `→` 화살표**(매너온도 `36.5°C → 37.0°C`) — 아이콘이 아니라 **글**이다.
  값과 값 사이를 잇는 문장부호라 아이콘으로 바꾸면 오히려 읽기 어려워진다.
- **첫 후기 알림의 `🎉`는 뺐다**(아이콘으로 바꾸지 않았다). 그 값은 알림 제목 **문자열**이라
  컴포넌트를 넣을 자리가 없고, 목록에서 그 줄만 그림이 붙으면 줄 높이가 어긋난다.
  "첫 후기"라는 사실은 본문("매너온도가 올랐어요")이 이미 말한다.

### 브랜드 표식은 직접 그렸다

lucide에 가지가 없다. 그렇다고 `🍆`를 두면 **서비스 이름 옆 표식이 보는 사람마다 다른
가지**가 된다 — 그건 표식이 아니다. `shared/ui/brandMark.tsx`에 열몇 줄짜리 SVG로 그렸다.

**여기만 `currentColor`를 안 쓴다.** 다른 아이콘은 글자색을 따라가는 것이 맞지만
(켜진 탭은 초록, 꺼진 탭은 회색), 브랜드는 어디에 놓이든 같은 색이어야 한다 —
**가지가 초록이면 가지가 아니다.**

기본 프로필 이미지의 `🍆`도 함께 걷어냈다. 닉네임이 없을 때 쓰던 값인데, 그건
"이 사람의 첫 글자"가 아니라 브랜드 표식이라 자리에 안 맞았다 — `User` 아이콘으로 바꿨다.

---

## 7. 이번에 안 한 것

| 안 한 것 | 왜 |
|---|---|
| **채팅 2단**(왼쪽 방 목록 + 오른쪽 대화) | 데스크탑 메신저의 기본 모양이지만 라우팅이 바뀐다 — 지금은 `/chats`와 `/chats/:id`가 **다른 화면**이라, 2단으로 만들면 두 주소가 한 화면을 가리키게 되고 뒤로가기 뜻도 달라진다. 폭만으로 되는 일이 아니다 |
| **지도 화면의 좌우 분할** | 같은 이유 + 카카오 지도 인스턴스를 다시 그려야 한다 |
| **사이드바 내비게이션** | 상단 바로 충분하다. 자리가 다섯뿐이라 세로로 세울 값이 없다 |
| **폼의 2단 배치** | 글쓰기·설정은 위에서 아래로 채우는 것이 순서 자체다. 좌우로 가르면 어디부터 채워야 할지 흐려진다 |
| **폰트·간격 스케일 조정** | 폭 문제와 별개다. 섞으면 무엇 때문에 달라 보이는지 못 가린다 |

---

## 8. 확인

- 테스트 **734개 통과**, lint·build 통과.
- 아이콘을 바꾸며 늘어난 번들은 **raw +8.5 kB / gzip +3 kB**다(787.6 → 796.1 kB).
  이름 있는 import만 써서 실제로 쓰는 스물몇 개만 들어간다.
- 빌드된 CSS에서 유틸리티 셋이 **미디어쿼리 안에 제대로 들어갔는지 확인했다.**

```
@media(min-width:48rem){.page-wide{max-width:var(--container-4xl)}
@media(min-width:64rem){.page-wide{max-width:var(--container-6xl)}
@media(min-width:48rem){.page-narrow{max-width:var(--container-2xl)}
@media(min-width:64rem){.page-detail{max-width:var(--container-5xl)}
```

- `AppHeaderNav` 테스트 일곱을 새로 붙였다(다섯 자리 · 현재 위치 · 하위 화면 ·
  안 읽은 배지 · 알림 종의 로그인 여부).

~~**아직 눈으로 못 봤다.**~~ — ✅ **2026-08-10에 아홉 건을 한 번에 훑었다.**
막고 있던 것이 도구가 아니라 **데이터**였다 — 채팅방 0·댓글 0·후기 0·알림 0이면 절반 이상이
빈 화면만 보여준다. `npm run seed`로 볼 것을 만들고 나서야 확인이 됐다.

저장소에 브라우저 자동화 도구는 여전히 없어(playwright·puppeteer·cypress 모두 없음)
실제 화면은 사람이 봐야 한다. 볼 때 이 순서가 빠르다.

```
0. 아이콘이 전부 선으로 그린 모양인가 (이모지가 남아 있으면 색이 튄다)
   특히 탭바에서 **켜진 탭의 아이콘이 초록으로 바뀌는지** — 이모지로는 안 되던 것이다
1. 창을 좁혔다 넓히며 768px을 지나 본다 — 하단 탭바와 상단 바가 서로 바뀌는가
2. 홈에서 로고와 알림 종이 한 번씩만 보이는가 (768px 위아래 각각)
3. 홈·검색 목록이 768px에서 2열, 1280px에서 3열이 되는가
4. 게시물 상세를 1024px 위에서 — 사진이 왼쪽에 붙어 따라오는가, 댓글이 아래 좁게 오는가
5. 채팅방과 글쓰기 폼이 넓은 화면에서 **과하게 벌어지지 않는가**
6. 마지막 줄이 하단 탭바에 가리지 않는가 (모바일) / 아래에 빈 띠가 없는가 (데스크탑)
```
