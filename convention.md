# 코딩 컨벤션

## TypeScript

- `any` 타입 사용을 금지한다.
- 화살표 함수 사용을 금지한다.

## 네이밍

- 파일명, 함수명은 camelCase를 사용한다.
- 상수는 `const` 키워드를 사용하고 대문자로 표기한다. (예: `const MAX_COUNT = 10`)

## 컴포넌트

- 컴포넌트는 역할별로 반드시 잘 분리한다.
